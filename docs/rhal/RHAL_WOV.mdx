---
sidebar_position: 95
---

# RHAL_WOV

RHAL_WOV api


## Function declarations and inline functions

### RHAL_WOV_SetScreenAttr
```bash
BOOLEAN RHAL_WOV_SetScreenAttr(BOOLEAN bOnOff);
```
Set screen attributes, specifically controlling the on/off state of the screen backlight.
<ParamTable
  items={[
    { name: 'bOnOff', type: 'BOOLEAN', desc: 'a boolean value controlling the on/off state' },
  ]}
/>
