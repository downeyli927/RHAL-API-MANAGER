---
sidebar_position: 11
---

# RHAL_BRIDGE

RHAL_BRIDGE api


## Function Declaration
### RHAL_Bridge_H2V_Burn_HDCP_1_4_RX_Key
```bash
DTV_STATUS_T RHAL_Bridge_H2V_Burn_HDCP_1_4_RX_Key(UINT8*key_buffer, UINT32 key_len);
```
This function is used to interact with the bridge chip via I2C to burn H2V_HDCP_1_4_RX into the slave device.
<ParamTable
  items={[
    { name: 'key_buffer', type: 'UINT8*', desc: 'The key data buffer' },
    { name: 'key_len', type: 'UINT32', desc: <>Length of the key data.</> },
  ]}
/>


### RHAL_Bridge_H2V_Burn_HDCP_1_4_TX_Key
```bash
DTV_STATUS_T RHAL_Bridge_H2V_Burn_HDCP_1_4_TX_Key(UINT8*key_buffer, UINT32 key_len);
```
This function is used to interact with the bridge chip via I2C to burn H2V_HDCP_1_4_TX into the slave device.
<ParamTable
  items={[
    { name: 'key_buffer', type: 'UINT8*', desc: 'The key data buffer' },
    { name: 'key_len', type: 'UINT32', desc: <>Length of the key data.</> },
  ]}
/>


### RHAL_Bridge_H2V_Burn_HDCP_2_2_RX_Key
```bash
DTV_STATUS_T RHAL_Bridge_H2V_Burn_HDCP_2_2_RX_Key(UINT8*key_buffer, UINT32 key_len);
```
This function is used to interact with the bridge chip via I2C to burn H2V_HDCP_2_2_RX into the slave device.
<ParamTable
  items={[
    { name: 'key_buffer', type: 'UINT8*', desc: 'The key data buffer' },
    { name: 'key_len', type: 'UINT32', desc: <>Length of the key data.</> },
  ]}
/>


### RHAL_Bridge_H2V_Burn_HDCP_2_2_TX_Key
```bash
DTV_STATUS_T RHAL_Bridge_H2V_Burn_HDCP_2_2_TX_Key(UINT8*key_buffer, UINT32 key_len);
```
This function is used to interact with the bridge chip via I2C to burn H2V_HDCP_2_2_TX into the slave device.
<ParamTable
  items={[
    { name: 'key_buffer', type: 'UINT8*', desc: 'The key data buffer' },
    { name: 'key_len', type: 'UINT32', desc: <>Length of the key data.</> },
  ]}
/>


### RHAL_Bridge_V2H_Burn_HDCP_1_4_RX_Key
```bash
DTV_STATUS_T RHAL_Bridge_V2H_Burn_HDCP_1_4_RX_Key(UINT8*key_buffer, UINT32 key_len);
```
This function is used to interact with the bridge chip via I2C to burn V2H_HDCP_1_4_RX into the slave device.
<ParamTable
  items={[
    { name: 'key_buffer', type: 'UINT8*', desc: 'The key data buffer' },
    { name: 'key_len', type: 'UINT32', desc: <>Length of the key data.</> },
  ]}
/>


### RHAL_Bridge_V2H_Burn_HDCP_1_4_TX_Key
```bash
DTV_STATUS_T RHAL_Bridge_V2H_Burn_HDCP_1_4_TX_Key(UINT8*key_buffer, UINT32 key_len);
```
This function is used to interact with the bridge chip via I2C to burn V2H_HDCP_1_4_TX into the slave device.
<ParamTable
  items={[
    { name: 'key_buffer', type: 'UINT8*', desc: 'The key data buffer' },
    { name: 'key_len', type: 'UINT32', desc: <>Length of the key data.</> },
  ]}
/>


### RHAL_Bridge_V2H_Burn_HDCP_2_2_RX_Key
```bash
DTV_STATUS_T RHAL_Bridge_V2H_Burn_HDCP_2_2_RX_Key(UINT8*key_buffer, UINT32 key_len);
```
This function is used to interact with the bridge chip via I2C to burn V2H_HDCP_2_2_RX into the slave device.
<ParamTable
  items={[
    { name: 'key_buffer', type: 'UINT8*', desc: 'The key data buffer' },
    { name: 'key_len', type: 'UINT32', desc: <>Length of the key data.</> },
  ]}
/>


### RHAL_Bridge_V2H_Burn_HDCP_2_2_TX_Key
```bash
DTV_STATUS_T RHAL_Bridge_V2H_Burn_HDCP_2_2_TX_Key(UINT8*key_buffer, UINT32 key_len);
```
This function is used to interact with the bridge chip via I2C to burn V2H_HDCP_2_2_TX into the slave device.
<ParamTable
  items={[
    { name: 'key_buffer', type: 'UINT8*', desc: 'The key data buffer' },
    { name: 'key_len', type: 'UINT32', desc: <>Length of the key data.</> },
  ]}
/>


### RHAL_Bridge_Firmware_Upgrade
```bash
DTV_STATUS_T RHAL_Bridge_Firmware_Upgrade(UINT8*fw_buffer, UINT32 fw_len);
```
This function is used to upgrade the firmware of the Bridge. The firmware is divided into two parts: TX and RX.


### RHAL_Bridge_Firmware_Upgrade_Ex
```bash
DTV_STATUS_T RHAL_Bridge_Firmware_Upgrade_Ex(UINT8*fw_buffer, UINT32 fw_len, UINT32 option);
```
