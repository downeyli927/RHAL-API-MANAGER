---
sidebar_position: 65
---

# RHAL_UART

RHAL_UART api

## RHAL_UART_GetDftCfg
```bash
extern DTV_STATUS_T RHAL_UART_GetDftCfg(struct termios*pCfg);
```

## RHAL_UART_SwitchUart
```bash
extern DTV_STATUS_T RHAL_UART_SwitchUart(SINT32 port_id);
```

## RHAL_UART_OpenUart
```bash
extern DTV_STATUS_T RHAL_UART_OpenUart(SINT32 port_id, struct termios*pCfg);
```

## RHAL_UART_CloseUart
```bash
extern DTV_STATUS_T RHAL_UART_CloseUart(SINT32 port_id);
```

## RHAL_UART_ReadUart
```bash
extern DTV_STATUS_T RHAL_UART_ReadUart(SINT32 port_id, UINT8*pbuff, SINT32*content_size, UINT32 read_size);
```

## RHAL_UART_WriteUart
```bash
extern DTV_STATUS_T RHAL_UART_WriteUart(SINT32 port_id, const UINT8*pbuff, UINT32 write_size);
```

## RHAL_UART_GetPortStatus
```bash
extern DTV_STATUS_T RHAL_UART_GetPortStatus(SINT32 port_id, PORT_STATUS_T*pStat);
```

## RHAL_UART_GetPortCfg
```bash
extern DTV_STATUS_T RHAL_UART_GetPortCfg(SINT32 port_id, struct termios*pCfg);
```

## RHAL_UART_SetPortCfg
```bash
extern DTV_STATUS_T RHAL_UART_SetPortCfg(SINT32 port_id, struct termios*pCfg);
```

## RHAL_UART_GetDftPort
```bash
extern DTV_STATUS_T RHAL_UART_GetDftPort(SINT32*port_id);
```
